# Dart pub workspace for the AssemblyOps Flutter web tier

name: assemblyops_web
description: "Workspace root for the AssemblyOps Flutter web apps and shared packages"
publish_to: none

environment: 
  sdk: ^3.11.0 

dev_dependencies:
  flutter_lints: ^6.0.0
  
