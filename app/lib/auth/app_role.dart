/// Application role assigned to the authenticated user.
///
/// Volunteer is the default for any signed-in user. Admin is granted via
/// allowlist on the admin portal.
enum AppRole { volunteer, admin }
